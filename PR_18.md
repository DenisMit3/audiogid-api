# PR 18: Mobile Web Client (Main Screen)

## CONTEXT PACK
A) **Non‑negotiables**: RU-only; **offline-first** (MapLibre + PWA basics); **premium UI/UX**; **glassmorphism**; **MapLibre GL JS**.
B) **Current scope**:
    *   Initialize PWA with Vite.
    *   Main Screen: Full-screen Map (MapLibre).
    *   UI Overlay: Bottom sheet (glass) with nearby tours.
    *   Integration: Fetch tours from `GET /v1/public/catalog?city=kaliningrad_city`.
C) **Aesthetics**: Dark mode, emerald/gold accents, blur effects.

## Implementation Plan
1.  **Dependencies**:
    *   `npm install maplibre-gl @capacitor/core @capacitor/android @capacitor/ios`.
2.  **Map Integration**:
    *   `index.html`: Setup viewport, link Google Fonts (Inter).
    *   `style.css`: Core design system (tokens, glass effects).
    *   `main.js`: Initialize MapLibre with a public style (OpenStreetMap-based or CartoDB Dark).
3.  **UI Components**:
    *   Floating Action Buttons (Location, Ingest).
    *   Scrollable drawer (simple JS/CSS) for Tour Cards.
4.  **Local Dev**:
    *   Proxy requests to Backend (or direct to Vercel).

## Verification
*   **Browser**: Open local Vite dev server.
*   **Mobile**: Test responsive layout (iPhone 14 viewport).

## Rollback
*   Delete `apps/mobile/`.
