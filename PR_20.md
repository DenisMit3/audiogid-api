# PR 20: Tour Details & Audio Player

## CONTEXT PACK
A) **Non‑negotiables**: RU-only; **offline-first**; **premium UI/UX**; **glassmorphism**.
B) **Current scope**:
    *   Implement Tour Details screen in `apps/mobile/`.
    *   Display POI list with "Locked/Unlocked" states based on entitlements.
    *   Implement Audio Player component:
        *   Play preview (always free).
        *   Play full narration (requires entitlement).
    *   Integration: `GET /v1/public/tour/{id}`.
C) **Aesthetics**: Album art blur, large play controls, emerald progress bars.

## Implementation Plan
1.  **Mobile Router/State**:
    *   Add basic routing to `main.js` (Catalog vs Tour Detail).
2.  **API Update**:
    *   Ensure `GET /v1/public/tour/{id}` returns correct media and POI status.
3.  **UI Components**:
    *   Tour Header with background image.
    *   POI List Item (Title, distance, play icon).
    *   Overlay Player (Minimized/Fullscreen).
4.  **Security**:
    *   Check `Entitlement` presence in API before returning full `narration_url`.

## Verification
*   **Browser**: Click a tour in Catalog -> View details.
*   **Audio**: Play preview audio.

## Rollback
*   Revert changes in `apps/mobile/`.
