{
    "framework": "fastapi",
    "buildCommand": "pip install -r requirements.txt --break-system-packages",
    "installCommand": "pip install -r requirements.txt --break-system-packages",
    "regions": [
        "fra1"
    ],
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "index.py"
        }
    ],
    "env": {
        "PYTHON_VERSION": "3.11"
    }
}