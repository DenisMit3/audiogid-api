name: billing_service
description: High-level billing service wrapper for Audiogid Flutter app with server-side verification.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  api_client:
    path: ../api_client
  in_app_purchase: ^3.1.0
  shared_preferences: ^2.2.0
  uuid: ^4.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0
